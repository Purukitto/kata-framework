---
"@kata-framework/test-utils": major
---

Stable v1.0.0 release. `createTestEngine`, `collectFrames`, `assertFrame`, `mockAudioManager`, and `StoryTestRunner` are now frozen. Future breaking changes require a new major version.
