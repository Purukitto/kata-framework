---
"create-kata-story": major
---

Stable v1.0.0 release. `bun create kata-story` templates (`minimal`, `react`, `multiplayer`), the programmatic `scaffold()`/`normalizeName()`/`validateName()` API, and the generated project structure are now frozen. Future breaking changes require a new major version.
