---
"@kata-framework/sync": major
---

Stable v1.0.0 release. `KataSyncManager`, `KataSyncTransport`, the built-in BroadcastChannel and WebSocket transports, authority tracking, choice policies, state partitioning, and the `KataServer` / `Room` server subpath are now frozen. Future breaking changes require a new major version.
