---
"@kata-framework/cli": major
---

Stable v1.0.0 release. The `kata` CLI's commands (`build`, `watch`, `graph`), flags, and `kata.config.json` schema are now frozen under semver. Future breaking changes require a new major version.
