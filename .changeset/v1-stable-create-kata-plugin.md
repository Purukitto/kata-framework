---
"create-kata-plugin": major
---

Stable v1.0.0 release. `bun create kata-plugin` scaffolding, the generated plugin project layout, and the programmatic `scaffold()`/`normalizeName()`/`validateName()` API are now frozen. Future breaking changes require a new major version.
